//Task nr.1:

//let ls=function(age, amount){
 //   (age=(85)-age),amount;
 //   let lifeamount=age*amount;
  // return lifeamount;}

//();
 //ls();
//ls();
//let age=prompt("Enter your age");
//let amount=prompt("Enter snacks calories");

//document.getElementById("output").innerHTML="You will need "+ ls(age, amount) + " to last until the age of 85.";

 


//let ls= (age,amount)=>
 //(85-(age))*(amount);
 //ls= ()=>
 //(85-(age))*(amount);
 //ls= ()=>
 //(85-(age))*(amount);

//let age=prompt("Enter your age");
//let amount=prompt("Enter the amount of calories");


//document.getElementById("output").innerHTML="You will need " + ls(age,amount) + " to last until the age of 85.";

//TASK NR. 2:
let conversion=function(C,F){
    (F=((C*(9))/5)+32), (C=(F-(32))/45);
return C,F; } 
let F=prompt("Enter today's temperature in C");
let C=prompt("Enter today's temperature in F.")

document.getElementById("output").innerHTML="Today's temperature is " + conversion(C) + " F or " + conversion(F)+ " C.";
 